int c = 'c';
int point = '.';
bool b = false;

b = 'a' <= c && c <= 'z';

if(!b) {
    write_char('e');
    write_char('r');
    write_char('r');
    write_char('o');
    write_char('r');
}
else {
    int e = 'o';
    int f = 'k';
    write_char(e);
    write_char(f);
}
    
write_char(point);
